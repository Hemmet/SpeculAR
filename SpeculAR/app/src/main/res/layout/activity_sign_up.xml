<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SignUpActivity" >

    <EditText
        android:id="@+id/userSignUpEmail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="160dp"
        android:ems="10"
        android:hint="@string/email_hint"
        android:inputType="textEmailAddress"
        android:singleLine="true" />

    <EditText
        android:id="@+id/userSignUpPassword"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/userSignUpEmail"
        android:layout_below="@+id/userSignUpEmail"
        android:layout_marginTop="30dp"
        android:ems="10"
        android:hint="@string/password_hint"
        android:inputType="textPassword"
        android:singleLine="true" />

    <Button
        android:id="@+id/signUpButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/userSignUpPassword"
        android:layout_alignEnd="@+id/userSignUpPassword"
        android:layout_below="@+id/userSignUpPassword"
        android:layout_marginTop="33dp"
        android:text="@string/sign_up_button_label" />

    <Button
        android:id="@+id/goLoginPageButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/signUpButton"
        android:layout_alignStart="@+id/userSignUpPassword"
        android:layout_alignEnd="@+id/userSignUpPassword"
        android:text="@string/already_have_an_acoount"
        android:background="@android:color/transparent"
        android:textAllCaps="false"/>

</RelativeLayout>